{
  "name": "frontend",
  "private": true,
  "version": "0.0.1",
  "description": "JukeBox for playing my music",
  "scripts": {
    "build": "stencil build",
    "start": "stencil build --dev --watch --serve",
    "test": "stencil test --spec --e2e",
    "test.watch": "stencil test --spec --e2e --watchAll",
    "generate": "stencil generate"
  },
  "devDependencies": {
    "@stencil/core": "2.4.0",
    "@stencil/router": "^1.0.1",
    "prettier": "^2.2.1"
  },
  "license": "MIT"
}
